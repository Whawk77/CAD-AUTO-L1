# AUTOFIXDIM / ASD 项目交接文档

最后同步日期：2026-05-26

## 目标
AutoCAD 2020 .NET Framework 插件，用于夹具零件半自动标注。主要命令为 `ASD`；旧命令保留以兼容。

插件执行以下标注：
- 完整包络轮廓的宽度和高度
- 选定的台阶和主体尺寸
- 主轮廓倒角和圆角标注
- 基于基准孔的孔位尺寸
- 销孔组和销孔间距（含公差文本）
- 交互式放置的直径、销孔和螺纹孔标注

不创建中心线或 `CENTER` 图层。

## 近期源码改动
- `DimensionDrawer.cs`：销孔组规划现在将用户选定的基准销孔作为第一组的 `BasePin`。后续组先识别，然后在组内使用前一组的 `BasePin` 作为定位参考来选择该组的 `BasePin`。
- `DimensionDrawer.cs`：销孔组基准尺寸从基准孔组基座的最近轮廓侧选择 Bottom/Top 和 Left/Right。同组销孔间距目前从两个销孔的中点选择侧。分配到销孔组的普通/螺纹孔目前使用该组的选定侧；只有无销孔回退时使用目标孔自身的最近侧。
- `DimensionDrawer.cs`：当孔在相反两侧之间在 `GeometryTolerance` 范围内居中时，销孔组放置回退到基准边的对侧，减少与基准侧尺寸的重叠。
- `DimensionDrawer.cs`：角特征引线预览/最终引线现在跟随当前 AutoCAD 图层和 `CECOLOR`；角特征 `MText` 使用直径/角标注标注样式的文本颜色。
- `DimensionDrawer.cs` 和 `FeatureRecognizer.cs`：内槽倒角识别现在要求内部垂直槽线的另一端连接到另一个 45 度倒角、已知倒角或圆角。这防止单条松散垂直线将长倾斜结构边误识别为内槽倒角。
- `DimensionDrawer.cs`：同侧重复测量尺寸现在按测量箭头间隔和跨度去重，不再要求相同的覆盖文本。当重复尺寸测量相同几何时，保留的尺寸优先选择：有显式公差文本的、有较小公差值的、功能尺寸类型优先级高的、强制外层的、有非空覆盖文本的。
- `DimensionDrawer.cs`：布局后共享延伸线对齐可将候选尺寸向外移动到最近合适的外部尺寸。阻挡规则检查实际 2D 箭头端点接触；仅共享同一 X 或 Y 坐标但在不同尺寸线层级不阻挡对齐。

## 项目
- 活动 L1 工作区：`D:\work\AI\project\L1`
- 活动源码：`D:\work\AI\project\L1\autocad-net-c-autocad-autocad-net-source-backup-20260522-1340`
- 项目文件：`AutoFixtureDim.csproj`
- 目标框架：.NET Framework 4.7.2
- AutoCAD 安装路径：`D:\Program Files\Autodesk\AutoCAD 2020`
- 编译命令：`dotnet msbuild AutoFixtureDim.csproj /p:Configuration=Debug /p:PostBuildEvent= /p:DebugType=None /p:DebugSymbols=false /v:minimal`
- 编译输出：`D:\work\AI\project\L1\autocad-net-c-autocad-autocad-net-source-backup-20260522-1340\bin\Debug\AutoFixtureDim.dll`
- 部署文件夹：`D:\app\不加班的小刘_工具箱\dll`
- 部署脚本：`.\deploy_next_version.ps1`
- 当前测试 DLL：`D:\work\AI\project\L1\autocad-net-c-autocad-autocad-net-source-backup-20260522-1340\bin\Debug\autofixdim-LB21.dll`
- 用户要求的重写前 `DimensionDrawer.cs` 备份：`D:\work\AI\project\1\DimensionDrawer.cs.bak`

## 命令
- `ASD`：主命令。
- `AUTOFIXDIM`：相同工作流程。
- `AUTOFIXDIMREGEN`：先清除旧插件标注，再重新生成。
- `AUTOFIXDIMCLEAR`：从非 XRef、非依赖块表记录中移除带有 XData 应用名 `AUTOFIXDIM` 的实体。

## 当前工作流程
1. 选择 `DRAWING` 图层上的轮廓。
2. 确认基准边，默认为轮廓 `MinX` / `MinY`，或指定基准点。
3. 窗口选择孔几何；插件从选择中识别圆、螺纹弧、槽弧和槽线。
4. 如果窗口选择未识别到孔，提示手动选择圆。
5. 如果存在销孔，提示用户选择一个销孔作为基准孔。
6. 如果选择了基准孔，提示该基准孔自身位置尺寸的 X 和 Y 参考点。
7. 每个基准 X/Y 点之后，提示公差模式：默认 `A` 表示无 `±0.05`；输入 `S` 添加 `<>±0.05`。
8. 仅从轮廓几何识别主轮廓倒角/圆角。
9. 将线性尺寸收集到 Bottom / Top / Left / Right 桶中。
10. 使用箭头/文字冲突避让、当前抑制规则和布局后共享延伸线对齐刷新堆叠尺寸。
11. 在任何交互式标注 Jig 开始前提交主自动线性尺寸。
12. 使用轻量级 `DrawJig` 放置倒角/圆角标注：移动鼠标预览，点击放置，Enter 跳过当前组，Esc 取消剩余角标注。
13. 使用轻量级 `DrawJig` 放置 U 槽半径标注：同半径槽分组，例如 `2x2-R3.5`。
14. 使用轻量级 `DrawJig` 放置直径/螺纹标注：按空间孔组顺序处理；每组内按销孔、螺纹孔、普通孔顺序标注。
15. 对于销孔直径标注，如果图纸已包含该块定义，则插入粗糙度块 `CadAider_国标粗糙度16下`。

## 源码地图
- `Commands.cs`：命令入口、主工作流程、线性后交互式标注协调和直径 Jig 放置排序。
- `GeometryCollector.cs`：轮廓选择、孔源收集、基准提示、基准孔选择提示。
- `FeatureRecognizer.cs`：轮廓/孔识别、主轮廓倒角/圆角识别、销标记匹配、螺纹弧/小圆逻辑、U 槽识别、行分组、诊断由 `DiagnosticsEnabled` 控制。
- `FeatureModels.cs`：`HoleFeature`、`HoleKind`、`DimensionType`、`OutlineSegment`、`OutlineArc`、`ChamferFeature`、`FilletFeature`。
- `RuleConfig.cs`：集中公差和标注文本，包括销孔 H7、销孔间距 `±0.02`、组间距 `±0.05`、基准孔定位 `<>±0.05` 和默认基准公差模式。
- `DimensionDrawer.cs`：线性位置尺寸、销孔组逻辑、堆叠放置、共享延伸线对齐、包络总尺寸、当前实验性台阶/突出部规则、倒角/圆角抑制、角特征 Jig 放置。
- `NativeDiameterDimensioner.cs`：最终交互式直径/螺纹尺寸、孔标注 Jig 预览、短 AcquirePoint 提示、销孔粗糙度块插入。
- `AnnotationMetadata.cs`：XData 标记和清除逻辑，应用名为 `AUTOFIXDIM`；生成的实体在 XData 中携带 `GroupId`。
- `LayerManager.cs`：首选标注图层是 `JEE-DIM标注`，否则使用当前图层。
- `DimStyleManager.cs`：如果当前标注样式是首选的 `SCALE-1-0x` 样式之一，则保持该样式；直径标注首先尝试所选线性样式的 `$3` 子样式，然后回退到 `SCALE-1-01$3`，然后是线性样式。
- `deploy_next_version.ps1`：将 `bin\Debug\AutoFixtureDim.dll` 部署为工具箱 DLL 文件夹中的下一个 `autofixdim-vNN.dll`。

## 规则配置
- 销孔直径公差基于规则。任何识别为 `HoleKind.Pin` 的孔都获得 `H7`。
- 同组销孔间距使用 `±0.02`。
- 销孔组间定位尺寸使用 `±0.05`。
- 基准孔 X/Y 位置公差默认为无公差；用户必须输入 `S` 添加 `<>±0.05`。
- 将未来规则调整首先放在 `RuleConfig.cs` 中，然后仅在规则需要新行为时才触碰绘图代码。

## 孔识别
- 普通孔：轮廓边界内的选定 `Circle`。
- 销孔：与 CadAider 销标记块匹配的选定 `Circle`。
- 用户选择的基准销可通过 `ObjectId` 匹配；如果失败，源码接受中心/半径公差匹配 `0.01`。如果选择的圆合理（`0.5 <= radius <= 100.0`）且在轮廓内，即使不在自动识别销孔列表中，也接受为手动基准销。
- 销标记匹配有意收紧：首先同心匹配，回退搜索半径 `max(radius * 0.0, 1.0)`。
- 螺纹孔：扫描角至少 270 度且有同心小圆的选定 `DRAWING` 弧。
- 螺纹小圆提供螺纹标注，并作为独立普通孔直径标注被抑制。
- 已知螺纹小径映射：
  - `4.917` / `5.0` -> `M6`
  - `6.647` / `6.8` -> `M8`
  - `8.376` / `8.5` -> `M10`
  - `10.106` / `10.2` -> `M12`
  - `13.835` / `14.0` -> `M16`
- 诊断输出默认通过 `DiagnosticsEnabled = false` 禁用；仅在针对性调试时重新启用。
- U 槽半弧在半圆约 15 度范围内接受。双弧槽要求半径匹配在 `max(GeometryTolerance, radius * 0.02)` 内，相同 X 或相同 Y 对齐约在 `radius * 0.05` 内，正好两条连接的 `DRAWING` 线。
- 单弧 U 槽也可从一个半弧加两条接触相对弧端点的平行线识别。它们的中心距为 `0`，因此不发出中心距尺寸；半径引线指向真实弧中点。
- 槽源实体在槽识别后从普通孔/圆处理中抑制。槽中心点仅在定位流程中表示为 `HoleKind.Slot`，不参与直径标注分组。
- U 槽识别检测由两条平行线加两个半圆弧组成的选定 `DRAWING` 图层槽。识别的槽优先通过发出两弧中心距尺寸，再仅对基准侧槽中心添加外部定位，并发出一对交互式 `2-Rx` 半径标注。不发出直径标注或 `8x17腰孔` 规格文本。
- 当不存在销孔时，U 槽定位使用从基准/参考点的连续尺寸链，而非从基准边重复每个槽。
- 当销孔组存在时，U 槽锚点定位使用基准侧槽中心，并从最近销参考定位；组基准销通过销组参考排序赢得平局决胜。

- 同半径 U 槽半径标注在放置时分组，例如两个匹配的 `2-R3.5` 槽合并为一个 `2x2-R3.5` 标注。
- 直径标注分组排除槽点，按空间孔组、`HoleKind`、直径、配合公差和螺纹标注文本分组。每组内交互顺序为销孔、螺纹孔、普通孔；之后处理下一空间组。
- 空间分组优先使用销簇结构，因此属于同一夹具孔组的孔放在一起。无销簇时回退使用当前行/空间顺序。

## 轮廓 / 包络尺寸
- 外轮廓选择针对 `DRAWING` 图层对象，并将实体线型视为 `ByLayer`；图层线型过滤中心/隐藏/虚线 construction geometry。
- 仍优先选择闭合多段线；否则 `GeometryCollector.SelectMainOutlineComponent` 使用图层语义、线宽、闭合/连续几何和边界范围选择最佳连续回退组件。
- 轮廓选择有意避免硬性 DXF 选择过滤器，以便旧 `POLYLINE` 实体和混合选择几何仍能到达项目诊断。
- 回退轮廓实体可能是线、弧、轻量多段线或旧 `Polyline2d` 实体。已确认的螺纹弧排除在回退轮廓候选之外。
- 回退组件评分首先优先图层语义（`DRAWING`，然后是包含 `OUTLINE`/`CONTOUR` 的图层名），然后是图层/实体线宽、闭合性、连续性计数、长度和边界面积。
- OverallWidth 始终是所选主轮廓包络的 `MinX -> MaxX`。
- OverallHeight 始终是所选主轮廓包络的 `MinY -> MaxY`，保持在左侧。
- 包络识别包括直线边、弧、圆角、倒角、多段线凸起和过渡边。弧上的基点被添加。
- 总尺寸具有最高优先级，不能被局部台阶、倒角切点或圆角切线尺寸替换。

## 倒角 / 圆角行为
- `OutlineFeature` 除了顶点、段和弧外还携带 `Chamfers` 和 `Fillets`。
- 倒角在连接一条水平主边和一条垂直主边的主轮廓 45 度非轴线段上识别。
- 如果第二个 45 度段与第一个共线且足够近以作为其延伸，则倒角值从合并端点的 `max(dx, dy)` 计算。
- 在正常倒角和圆角识别之后，`RecognizeInnerGrooveChamfers(outline)` 补充缺失的内槽倒角。这故意在 `RecognizeFillets(outline)` 之后，以便识别倒角+内直线+圆角结构。
- 内槽倒角补充仅接受参与内部水平或垂直槽关系的局部 45/135 度段。它不应将长结构倾斜边变为倒角标注。
- 对于垂直内槽关系，内部垂直线的另一端必须连接到另一个倒角/45 度段或圆角。这将补充限制在真实的倒角-线-倒角或倒角-线-圆角槽结构。
- 倒角标注文本遵循直径/角标注样式的单位线性精度（`Dimdec`）处理非整数；整数倒角值抑制尾随小数，例如 `C5` 而非 `C5.00`。
- 不是局部 45 度倒角几何的长结构倾斜边保持为 `Inclined Edge`，而非 `Chamfer`。
- 圆角仅从具有合理半径和连接轮廓端点的主轮廓弧或多段线凸起识别。
- 匹配的倒角/圆角文本分组为重复标注，例如 `2-C1` 和 `2-R3`。
- 倒角箭头附着在真实倒角边中点。
- 圆角/半径箭头附着在真实弧上，而非虚拟角点。
- 角特征放置使用轻量级 `DrawJig` 预览。预览显示简化的引线/文字反馈；最终 U 槽半径标注使用 `Leader` + `MText`，因此放置点不受 `RadialDimension` 约束。

## 抑制规则
- 特征抑制有意收窄。
- 仅抑制直接重复倒角/圆角的尺寸：理论尖角尺寸、切点尺寸、小特征局部跨度以及可完全推导的特征尺寸。
- 不抑制 OverallWidth、OverallHeight、主体长度/高度、功能定位尺寸或无关局部尺寸。
- 加宽的单切点圆角检查防止在端圆角附近发出错误局部切点尺寸，而预期外侧尺寸应包含半径。
- Top/Bottom 镜像重复水平正常尺寸跨侧去重；默认保留 Bottom。
- Left/Right 镜像重复垂直正常尺寸在 Y 间隔和覆盖文本匹配时跨侧去重；默认保留 Left。总体高度、强制外层尺寸和非正常特征尺寸不受此规则抑制。
- 同侧重复尺寸在其箭头间隔和测量跨度在 `GeometryTolerance` 内匹配时被视为重复，即使其覆盖文本不同。保留的重复尺寸由 `CompareDuplicatePreference` 选择。
- 同侧重复偏好顺序为：保留有公差文本的尺寸优先于无公差文本的；当两者都有公差文本时，保留较小提取公差值（如 `±0.02` 优先于 `±0.05`）；然后按功能尺寸类型优先级：`PinDistance`、`PinGroupDistance`、基准孔定位、总宽/高、normal；然后偏好 `ForceOuterLevel`；然后偏好任何非空覆盖文本。
- 公差提取有意同时识别正确的 `±` 字符和常见的乱码 `卤` 字符，以便重复保留器仍可针对遗留编码覆盖字符串工作。
- 在第一次堆叠布局传递之后，线性尺寸可通过一条延伸线与另一外部尺寸的延伸线重叠而向外对齐到最近合适的外部尺寸。这适用于例如 `50±0.02` 通过共享延伸线对齐到 `124±0.05` 的情况。
- 当候选的一个真实尺寸线箭头端点与另一尺寸箭头端点有 2D 点接触时不对齐。相同 X 不同 Y，或相同 Y 不同 X，不构成接触，不得阻挡对齐。
- 两个倒角之间的局部水平/垂直边缘当可从总包络减去两个倒角投影推导时被抑制。
- 废弃的 `v89` 规则试图将长总尺寸减倒角宽度如 `55` 替换为倒角投影 `5`；已被拒绝并从源码中移除。

## 台阶 / 突出部尺寸
- 当前 `DrawRightStepHeight` 是 2026-05-21 的实验性重写。它不再直接从垂直段发出一个右侧高度候选。
- 它按 Y 级别分组水平轮廓段，取每个级别的最左侧点，在相邻级别之间发出左侧连续垂直链，然后移除具有最长延伸线伸出的候选以避免闭合尺寸链。
- 此重写故意将这些台阶链尺寸放在左侧。
- `DrawRightSideStepHeight` 分别从水平轮廓级别的最右侧点构建右侧垂直链。左侧忽略的点不流入右侧忽略的点。
- 顶部和底部水平台阶宽度规则现在使用侧特定结构点集。Top 仅允许上半部内槽倒角端点进入 Top 点集；Bottom 仅允许下半部内槽倒角端点进入 Bottom 点集。
- 左右垂直台阶链使用匹配的内槽行为：对于侧内槽，被忽略的端点是连接到槽内部垂直线的倒角端点。
- 方向性 45/135 度端点抑制应保留在 `AddDirectionalInclinedEndpoint`、`AddSideDirectionalInclinedEndpoint` 和 `IsInnerGroove...` 辅助方法家族中。避免在绘图例程中添加一次性忽略点。
- 底部水平突出部宽度不再盲目选择最长内部水平段；它针对下方局部突出部，并可从附近垂直边界解析跨度。
- 用户要求的 `DrawRightStepHeight` 重写前的源码备份位于 `D:\work\AI\project\1\DimensionDrawer.cs.bak`。
- 台阶/突出部规则仍在 AutoCAD 可视测试中。优先调整分类器/链逻辑而非添加孤立的最长段启发式。

## 孔位原则

### 销孔
- 销孔在任何普通/螺纹孔位置尺寸发出之前分组。
- 第一个销孔组从用户选择的基准销孔开始（如果有），该选定的销孔必须保持为第一组的 `BasePin`；否则使用左侧/底部销孔作为回退基准。
- 销孔组目前包含种子销加上最近的同直径配对销（如果有）。后续组从前一基准销最近的剩余销开始种子；形成对后，使用前一组的 `BasePin` 从组内选择该组的 `BasePin`。
- 第一个销孔组基准销从轮廓/基准边定位。
- 当前源码将第二和后续销孔组基准销从第一销孔组基准销定位，而非从紧邻前一组或重复从轮廓定位。
- 销孔组间定位尺寸使用 `±0.05`。
- 同组销孔间距从组基准销到组内每个其他销发出，使用 `±0.02`。
- 销孔组孔位尺寸从组基准销选择输出侧：水平尺寸按最近轮廓侧去 Bottom/Top，垂直尺寸按最近轮廓侧去 Left/Right。
- 一旦组的 `BasePin` 固定，同组销孔间距从该基准销到其他销发出，但其侧从销对中点选择。
- 同组销孔间距不得受另一组侧规则影响；组间尺寸使用目标组的选定 `BasePin` 侧。
- 如果销孔组基准销在几何上在相反两侧之间居中，其孔位尺寸使用基准边的对侧。
- 跳过零长度尺寸。

### 普通孔和螺纹孔
- 普通孔不是高精度销孔。
- 普通孔位置尺寸不获得 `±0.02` 或 `±0.05`。
- 普通孔不建立或传递销孔组基准。
- 普通孔按到组前两个销的距离之和分配到最近的销对/组；平局时按到组 `BasePin` 的距离解析。
- 螺纹孔遵循与普通孔相同的销孔组分配原则。
- 螺纹孔永远不成为销孔组基准，不参与销孔组基准传递。
- 如果不存在销孔，普通/螺纹孔回退到轮廓/基准边定位。
- 存在销孔组时，普通/螺纹孔从assigned组 `BasePin` 定位并放置在该组的选定侧。没有销孔组时，普通/螺纹孔从轮廓/基准边定位并放置在目标孔最近轮廓侧。

## 直径 / 粗糙度标注
- 直径/螺纹标注放置使用轻量级 `DrawJig` 预览以避免缓慢的 `DiametricDimension` 动态块重绘。
- 直径/螺纹标注放置仅在主线性尺寸生成并提交后才开始。
- 最终标注仍是真实的 AutoCAD `DiametricDimension` 实体。
- 直径/螺纹标注 Jig 每组打印一次完整标注指令，然后在移动时使用短 `Pick point:` AcquirePoint 提示。预览文字点默认为 `center + radius * 3` 的 X 和 Y；Enter 跳过当前直径组，Esc/取消停止剩余直径组。
- 直径/螺纹放置顺序按空间组逐组。每组内，销孔直径标注优先，然后螺纹标注，然后普通孔直径标注；之后处理下一空间组。
- 角特征引线对预览和最终引线都使用当前 AutoCAD 图层和当前实体颜色（`CECOLOR`）。
- 角特征标注文字保留在标注图层上，但使用活动直径/角标注样式的文字颜色。
- 如果当前图纸中存在块定义，销孔直径标注插入块 `CadAider_国标粗糙度16下`。
- 粗糙度块旋转 180 度并标有 `AUTOFIXDIM` XData。
- 当前实现在最终尺寸文字位置插入粗糙度块。如果用户需要真实水平尺寸线的中点几何，请检查生成的匿名尺寸块并推导最长水平线段中点。

## 当前代码状态
- `DimensionDrawer.DrawHolePositionFromDatumHole` 包含活动的基准孔路径。
- `BuildPinGroupPlan` 构建销孔组基准链。选定的基准销锚定第一组作为其 `BasePin`；每组是种子加上可用的最近同直径配对销，后续组在形成对后使用前一组的 `BasePin` 从组内选择基准销。
- `AssignPinGroupPlacementSides` 在孔位尺寸发出之前选择每个销孔组的水平和垂直输出侧。
- `EmitFirstPinGroupBaseLocation` 从所选侧的轮廓/基准边定位第一组基准。
- `EmitPinGroupBaseTransfers` 目前从第一组基准定位每个后续组基准在目标组的所选侧。
- `EmitSameGroupPinDistances` 发出同组销孔间距 `±0.02`，并从正在定位的两个销的中点选择输出侧。
- `AssignNonPinHolesToNearestPinPair` 按销距和将普通/螺纹孔分配到最近销对，然后 `EmitNonPinHoles` 从该组的 `BasePin` 在该组的所选侧定位它们。
- `DrawSlotDimensions` 发出 U 槽中心距尺寸，然后 `DrawSlotAnchorLocations` 仅对基准侧槽中心发出外部定位尺寸对。
- `DrawSlotDimensions` 在没有销孔时使用连续槽定位。
- `AddSingleArcSlotHorizontalDatumDimension` 处理没有销孔组的水平单弧槽，通过在覆盖文本中测量真实中心到基准的跨度，同时将延伸点附着到真实轮廓点和最近上/下弧抓点。
- `PickSlotAnchorPoint` 按几何选择基准侧槽中心：水平槽选择更接近 `datum.BaseX` 的中心；垂直槽选择更接近 `datum.BaseY` 的中心。
- `DrawSlotRadiusLeadersWithJig` 将同半径 U 槽半径标注分组，例如 `2x2-R3.5`。
- `SuppressDuplicateMeasuredDimensions` 在按跨度排序后运行在每侧的 `FlushSide` 内部。它通过几何去除同侧重复测量尺寸，并使用 `CompareDuplicatePreference`、`GetDimensionPreferenceRank` 和 `TryExtractTolerance` 来保留最具规则意义的候选。
- `AlignDimensionsBySharedExtensionLines` 在堆叠放置后运行，可通过共享延伸线将候选对齐到最近合适的外部尺寸。
- `HasArrowEndpointTouch` 仅在实际 2D 箭头端点在尺寸线上接触时阻挡该对齐，而非当端点仅共享一个坐标时。
- `Commands.DrawPostLinearInteractiveAnnotations` 在自动线性尺寸事务提交后运行倒角/圆角、U 槽半径和直径/螺纹 Jig 放置。
- `Commands.BuildDiameterGroupsForPlacement` 按空间排序直径/螺纹 Jig 组，然后在每组内按销/螺纹/普通排序。
- `RecognizeOutlineCornerFeatures` 填充 `outline.Chamfers` 和 `outline.Fillets`。
- `RecognizeChamfers` 存储数值倒角 `Value`；倒角显示文本在绘制时从活动直径/角标注样式的线性精度重新格式化，整数值不显示尾随小数。
- 倒角延伸处理可在计算倒角值之前合并附近共线 45 度段。
- `DrawCornerFeatureLeadersWithJig` 处理倒角/圆角标注放置。
- `DrawRightStepHeight` 目前从水平轮廓级别发出左侧连续垂直链并移除最长延伸线候选，避免闭合尺寸链。
- `DrawLowerRightStepWidth` 针对下方局部突出部宽度，可从附近垂直边界解析端点。
- `NativeDiameterDimensioner.PromptDiameterDimensions` 处理直径/螺纹标注放置、一次性完整标注消息、短 AcquirePoint 提示和销粗糙度块插入。
- `FeatureRecognizer.DiagnosticsEnabled` 控制孔、槽、倒角和圆角的诊断，在当前源码中为 false。

## 已知问题 / 跟进
- 台阶尺寸规则正在积极重新设计。当前 LB21 源码使用实验性四侧点集和内槽端点规则；在将其视为稳定之前需要 AutoCAD 验证。
- 下一个持久设计仍应在发出尺寸之前对台阶候选进行分类：`Overall`、`FeatureDerived`、`FeatureProjection`、`StructuralStep` 和 `AmbiguousSmallStep`。
- 交接文档现已与当前源码行为对齐，其中 `EmitPinGroupBaseTransfers` 为所有后续组引用第一销孔组基准。如果预期设计是真正的前一组链，请仔细更改代码并同时更新本文档。
- 交接文档也与当前源码行为对齐，其中同组销孔间距使用中点侧选择，分配到销孔组的普通/螺纹孔使用所在组的侧。如果未来可视化测试期望每个目标最近侧，请仔细调整 `EmitSameGroupPinDistances` / `EmitNonPinHoleLocations`。
- 用户偏好不保留废弃的 `v89` 替换规则。不要重新引入将长结构宽度替换为倒角投影的行为。
- 注意意外的闭合尺寸链。当前实验规则删除具有最长延伸线伸出的候选，但这应针对真实夹具轮廓进行验证。
- 验证下方突出部宽度和倒角延伸值是否与用户预期的蓝色引导尺寸匹配。
- 如果文字位置锚定在视觉上不正确，粗糙度块放置可能仍需要从生成的尺寸块中提取精确水平线段中点。
- `AnnotationPreview.cs` 是遗留支持；活动的孔和角放置流程使用 `DrawJig`。
- 当前来自源码的最新测试 DLL 是 `bin\Debug\autofixdim-LB21.dll`；它使用 `DebugSymbols=false` / `DebugType=None` 编译，因为 AutoCAD 可能锁定 `bin\Debug\AutoFixtureDim.pdb`。
- 布局后共享延伸线对齐仍需要针对真实图纸进行可视化验证。关键验收案例是：小尺寸通过共享延伸线与外部尺寸对齐时，当其箭头在 2D 中实际不接触另一尺寸的箭头时向外对齐。
- 直径/螺纹空间分组假设销簇定义自然孔组。验证无销图纸仍以合理的行/空间顺序放置标注。

## 验证
- 标准编译命令：
  `dotnet msbuild AutoFixtureDim.csproj /p:Configuration=Debug /p:PostBuildEvent= /p:DebugType=None /p:DebugSymbols=false /v:minimal`
- 如果 AutoCAD 锁定 `bin\Debug\AutoFixtureDim.pdb`，使用带版本号的 no-PDB 构建文件夹，例如：
  `dotnet msbuild AutoFixtureDim.csproj /p:Configuration=Debug /p:DebugSymbols=false /p:DebugType=none /p:OutputPath=bin\Debug\vNNNbuild\ /v:minimal`
- 标准部署命令：
  `powershell -ExecutionPolicy Bypass -File .\deploy_next_version.ps1`
- 最新编译 DLL：
  `D:\work\AI\project\L1\autocad-net-c-autocad-autocad-net-source-backup-20260522-1340\bin\Debug\AutoFixtureDim.dll`
- 最新测试 DLL：
  `D:\work\AI\project\L1\autocad-net-c-autocad-autocad-net-source-backup-20260522-1340\bin\Debug\autofixdim-LB21.dll`

## AutoCAD 人工测试清单
- 使用 `NETLOAD` 加载 `D:\work\AI\project\L1\autocad-net-c-autocad-autocad-net-source-backup-20260522-1340\bin\Debug\autofixdim-LB21.dll` 进行当前本地测试构建。
- 运行 `ASD`。
- 选择轮廓和孔几何。
- 选择预期的第一组基准销。
- 对于基准孔 X/Y 公差提示，按 Enter 或输入 `A` 无公差；输入 `S` 添加 `<>±0.05`。
- 确认销孔直径标注始终包含 `H7`，例如 `2-%%c8H7`。
- 确认同组销孔间距显示 `±0.02`。
- 确认销孔组间定位尺寸显示 `±0.05`。
- 确认后续销孔组基准传递目前从第一销孔组基准测量，与源码行为匹配。
- 确认普通孔不获得 `±0.02` 或 `±0.05`。
- 确认螺纹孔不获得销孔组公差规则。
- 确认无销普通/螺纹回退尺寸移动到最近合适侧：下方孔到 Bottom，上方孔到 Top，左侧孔到 Left，右侧孔到 Right。
- 确认分配到销孔组的普通/螺纹孔使用所在销孔组的所选侧。
- 确认居中销孔组使用基准边对侧而非与基准侧尺寸碰撞。
- 确认每个 U 槽发出两中心距和恰好一对到基准侧槽中心的外部定位。
- 确认单弧 U 槽不发出中心距尺寸，仍发出半径引线标注，在没有销孔组时将基准尺寸附着到真实轮廓/弧抓点。
- 确认水平 U 槽选择更接近 `datum.BaseX` 的中心；垂直 U 槽选择更接近 `datum.BaseY` 的中心。
- 确认 U 槽点不产生直径标注或 `8x17腰孔` 文本。
- 确认同半径 U 槽半径标注分组，例如 `2x2-R3.5`。
- 确认没有发出零长度尺寸。
- 确认同侧重复测量尺寸保留规则重要的覆盖文本而非保留第一个/最短文本重复：`±0.02` 应优先于 `±0.05`，有公差覆盖应优先于空白文本，且 pin/datum/overall 类型不应丢失给测量相同跨度的普通正常尺寸。
- 确认 OverallWidth/OverallHeight 测量包含倒角和圆角的完整包络。
- 确认 `DrawRightStepHeight` 从水平轮廓级别发出左侧垂直链，移除最长延伸线候选，并避免闭合尺寸链。
- 确认下方突出部宽度从预期的局部垂直边界跨度发出，而非从最长内部水平段发出。
- 确认 45 度倒角在存在合并共线延伸段时使用合并段，整数值不显示尾随小数，而非整数遵循标注样式线性精度。
- 确认内槽倒角识别不将长结构倾斜边分类为倒角，除非内部垂直槽线在其另一端连接到另一倒角/圆角。
- 确认倒角/圆角引线附着到真实几何。
- 确认角特征引线跟随当前图层/当前 `CECOLOR`，而标注文字跟随直径/角标注样式颜色。
- 确认正常运行时不打印 `[diagnostic]` 或 `[诊断]` 孔/倒角/圆角日志。
- 确认倒角/圆角、U 槽半径和直径/螺纹 Jig 提示仅在自动线性尺寸生成并提交后才开始。
- 确认直径/螺纹 Jig 顺序按空间组逐组，每组内销孔优先于螺纹孔优先于普通孔。
- 确认布局后共享延伸线对齐将符合条件的尺寸向外移动，但不移动其真实箭头端点与同一尺寸线层级上另一箭头端点有 2D 点接触的尺寸。
- 确认孔标注命令行每组打印一次完整指令，并在 Jig 移动时使用短 `Pick point:` 提示。
- 测试角标注和直径/螺纹标注的 Jig 放置。
- 确认销孔粗糙度块仅在图纸中存在 `CadAider_国标粗糙度16下` 块时出现。
- 确认 `AUTOFIXDIMCLEAR` 仅移除生成的尺寸/标注/粗糙度块。
